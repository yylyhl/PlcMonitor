<Window x:Class="PlcMonitor.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlcMonitor.Wpf"
        mc:Ignorable="d"
        Title="PLC Monitor Wpf" Height="450" Width="800">

    <Window.Resources>
        <!-- 定义自定义按钮样式 -->
        <Style x:Key="CustomButtonStyle" TargetType="Button">
            <Setter Property="BorderBrush" Value="DarkBlue"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- 定义触发器 -->
            <Style.Triggers>
                <!-- 当按钮禁用时 -->
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="Gray"/>
                    <Setter Property="Foreground" Value="DarkGray"/>
                    <Setter Property="BorderBrush" Value="LightGray"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Background" Value="#3DCD58"/>
                    <Setter Property="Foreground" Value="#FFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding Status}" FontSize="16" Margin="0,0,0,20"/>
            <Button Content="连接" Command="{Binding ConnectCommand}" IsEnabled="{Binding ConnectButtonStatus}" Style="{StaticResource CustomButtonStyle}" Width="120" Name="ConnectButton" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding Temperature}" FontSize="24" Margin="0,20,0,0"/>
        </StackPanel>


    </Grid>
</Window>
